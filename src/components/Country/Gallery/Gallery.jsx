import React from 'react';
import classes from './Gallery.module.scss'

const Gallery = () => {
  return (
    <section>
      Gallery (фотогалерею достопримечательностей страны
      (не меньше шести) с названием
      и небольшим описанием каждой из них)
    </section>

  );
};

export default Gallery;